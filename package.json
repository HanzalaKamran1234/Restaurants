{
  "name": "ziyafat-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Ziyafat Premium Online Food Delivery Platform",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "db:push": "npx --prefix frontend prisma db push",
    "db:seed": "npx --prefix frontend prisma db seed"
  }
}
